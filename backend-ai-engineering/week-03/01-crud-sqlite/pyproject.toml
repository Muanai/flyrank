[project]
name = "02-crud-sqlite"
version = "0.1.0"
description = "FastAPI CRUD with SQLite"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.139.2",
    "uvicorn>=0.51.0",
]
